<?php
/**
 * نسخه فرد سی‌ام‌اس
 *
 * در یک فایل جدا نگه داشته می‌شود تا در به‌روزرسانی‌ها همراه بقیه کد
 * جایگزین شود؛ config.php روی سرور کاربر دست‌نخورده می‌ماند چون
 * تنظیمات اختصاصی او را دارد.
 *
 * به همین دلیل این فایل از includes/db.php بارگذاری می‌شود، نه از
 * config.php. هر فایل هسته‌ای که تازه اضافه می‌شود باید از یک فایلِ
 * همیشه‌بارگذاری‌شده صدا زده شود، وگرنه نصب‌های موجود که config.php
 * قدیمی دارند با خطای «ثابت تعریف‌نشده» متوقف می‌شوند.
 */

if (!defined('FARDCMS_VERSION')) {
    define('FARDCMS_VERSION', '1.1.0');
}
