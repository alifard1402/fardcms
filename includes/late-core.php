<?php
/**
 * فایل‌های هسته‌ای که پس از انتشار نسخه ۱.۰.۰ اضافه شده‌اند
 *
 * ┌───────────────────────────────────────────────────────────────┐
 * │  هر فایل هسته‌ای جدید را اینجا اضافه کنید، نه در config.php.   │
 * └───────────────────────────────────────────────────────────────┘
 *
 * دلیل: config.php متعلق به کاربر است و در به‌روزرسانی جایگزین نمی‌شود،
 * چون تنظیمات دیتابیس او داخلش است. اگر require یک فایل تازه فقط به
 * config.php اضافه شود، نصب‌های موجود آن را هرگز بارگذاری نمی‌کنند و با
 * «ثابت تعریف‌نشده» یا «تابع تعریف‌نشده» می‌شکنند — در حالی که نصب تازه
 * بی‌عیب کار می‌کند و اشکال دیر پیدا می‌شود.
 *
 * این فایل از includes/db.php بارگذاری می‌شود که در تمام نسخه‌های
 * config.php حضور دارد. تست tests/upgrade-test.sh همین را می‌سنجد.
 *
 * فایل‌های اینجا نباید در زمان بارگذاری کاری جز تعریف تابع و ثابت
 * انجام دهند، چون پیش از بقیه هسته بارگذاری می‌شوند.
 */

// قلاب‌ها باید پیش از هر چیز دیگری تعریف شوند تا بقیه هسته بتواند
// نقاط اتصال خود را اعلام کند
require_once __DIR__ . '/hooks.php';
require_once __DIR__ . '/version.php';
require_once __DIR__ . '/sanitizer.php';
require_once __DIR__ . '/asset-stamp.php';
require_once __DIR__ . '/plugins.php';
require_once __DIR__ . '/package.php';
