# کد قالب‌ها فقط از داخل سیستم اجرا می‌شود، نه با درخواست مستقیم.
# فایل‌های ظاهری (js و css و تصویر) باید در دسترس بمانند.
<FilesMatch "\.(php|phtml|php[0-9]|inc)$">
    Require all denied
</FilesMatch>
